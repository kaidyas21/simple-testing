pipeline {
    //Agent -another remote machine
    // Jenkins has master -node
    agent any

    stages{
        stages('Checkout'){
            steps{
                checkout scm
            }

        }

    }


}